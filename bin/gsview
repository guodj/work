#!/bin/bash
if [ "$#" == 1 ];then
    if [[ "$1" =~ '/' ]];then
        $HOME/opt/gsview/gsview $1 2> /dev/null &
    else
        $HOME/opt/gsview/gsview $PWD/$1 2> /dev/null &
    fi
elif [ "$#" == 0 ];then 
    $HOME/opt/gsview/gsview 2> /dev/null  &
fi
